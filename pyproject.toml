[project]
name = "ai-rag-prompteng"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.extra-build-dependencies]
openai-whisper = ["setuptools", "wheel"]
