[project]
name = "ai-rag-prompteng"
version = "0.1.0"
description = "Personalized Meeting Assistant — Transcribe, summarize, and query meeting recordings and videos with AI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
